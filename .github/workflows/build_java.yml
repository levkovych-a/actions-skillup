name: test-workflow
on:
  push:
    branches:
      - 'main'
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: checkout/checkout@v2
      - name: echo command
        run: echo 'It just works.' 
